/**
 * MasteringProcessor — STUB, not yet implemented.
 *
 * Future: will handle dynamic range compression and loudness normalization
 * based on target specs (LRA, true peak, LUFS).
 *
 * @see master.ts for the low-level ffmpeg loudnorm wrapper.
 */
export class MasteringProcessor {
  // TODO: implement analyzeDynamicSpecs + adjustCompression pipeline
}
