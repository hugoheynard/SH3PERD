import { StyledCheckboxDirective } from './styled-checkbox.directive';

describe('StyledCheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new StyledCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
