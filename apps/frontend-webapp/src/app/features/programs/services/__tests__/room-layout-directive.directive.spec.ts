import { RoomLayoutDirectiveDirective } from '../room-layout-directive.directive';

describe('RoomLayoutDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RoomLayoutDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
