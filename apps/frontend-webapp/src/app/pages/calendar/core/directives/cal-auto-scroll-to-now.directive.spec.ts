import { CalAutoScrollToNowDirective } from './cal-auto-scroll-to-now.directive';

describe('CalAutoScrollToNowDirective', () => {
  it('should create an instance', () => {
    const directive = new CalAutoScrollToNowDirective();
    expect(directive).toBeTruthy();
  });
});
