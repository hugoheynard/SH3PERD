import { DragStyleDirective } from '../drag-style.directive';

describe('DragStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new DragStyleDirective();
    expect(directive).toBeTruthy();
  });
});
