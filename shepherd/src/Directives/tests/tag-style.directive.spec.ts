import { TagStyleDirective } from '../tag-style.directive';

describe('TagStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new TagStyleDirective();
    expect(directive).toBeTruthy();
  });
});
