
const listNames = ["Hugo", "Antoine", "Tanguy"]
const whoCantFinish =  "Antoine";

const offset = listNames.indexOf(whoCantFinish);


const hasStartedThePreviousDay = "Tanguy"


const rotNumber = 4





const order = []

//push everybody everywhere
for (let i = 0; i < rotNumber; i++) {

        order.push(listNames)
}

const removeNoneFinishers = (array, nonFinishers) => {

        return array;
}

removeNoneFinishers(order, whoCantFinish)

console.log(order)

const testDJ = array => {

}