{
  "name": "@sh3pherd/auth",
  "version": "1.0.0",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsc -b tsconfig.json && tsc-alias && tsc-esm-fix dist --verbose",
    "dev:watch": "concurrently \"tsc -b tsconfig.json --watch\" \"tsc-alias --watch\" ",
    "test": "jest",
    "clean": "find . \\( -type d -name dist -o -name .turbo -o -name .cache \\) -exec rm -rf {} + && find . -type f -name '*.tsbuildinfo' -delete"
  }
}