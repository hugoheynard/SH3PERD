{
  "name": "@sh3pherd/bootstrap",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev:watch": "concurrently \"tsc -p tsconfig.json --watch\" \"tsc-alias --watch\" \"nodemon --watch 'dist/**/*.js' --exec 'node --inspect=9229 dist/app.js'\"",
    "test": "jest"
  }
}